#!/usr/bin/env ruby

TODO_PATH = File.join(Dir.home, "me/notes/Viktig")

if File.file?(TODO_PATH) && %x{wc -l #{TODO_PATH}}.split.first.to_i > 2
  puts "\n  Remember.."
  system "tail -n+2 #{TODO_PATH} | awk '$0=\"  \"$0'"
end
